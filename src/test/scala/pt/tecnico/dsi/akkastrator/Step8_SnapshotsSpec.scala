package pt.tecnico.dsi.akkastrator

class Step8_SnapshotsSpec extends ActorSysSpec {
  //Test:
  // · Ensure that when saveSnapshotRoughlyEveryXMessages == 0 no snapshots are saved
  // · Ensure that when saveSnapshotRoughlyEveryXMessages != 0 snapshots are saved at the appropriate time
}
